import { ContactButtonDirective } from './contact-button.directive';

describe('ContactButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new ContactButtonDirective();
    expect(directive).toBeTruthy();
  });
});

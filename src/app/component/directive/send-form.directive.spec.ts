import { SendFormDirective } from './send-form.directive';

describe('SendFormDirective', () => {
  it('should create an instance', () => {
    const directive = new SendFormDirective();
    expect(directive).toBeTruthy();
  });
});
